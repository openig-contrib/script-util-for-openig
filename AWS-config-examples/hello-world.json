{
  "handler": {
    "name": "myScriptableHandler",
    "type": "ScriptableHandler",
    "config": {
      "type": "application/x-groovy",
      "file": "hello-world.groovy"
    }
  },
  "condition": "${find(request.uri.path, '^/hello-world')}",
  "capture": "all"
}
